/*
  # Strict Transport Security
  Strict-Transport-Security: max-age=31536000; includeSubDomains

  # Content Security Policy
  Content-Security-Policy: default-src 'self' https://*.trello.com; script-src 'self' 'unsafe-inline' 'unsafe-eval' https://*.trello.com https://p.trellocdn.com https://cdnjs.cloudflare.com; style-src 'self' 'unsafe-inline' https://cdnjs.cloudflare.com; img-src 'self' data: https:; connect-src 'self' https://hub.apilabz.com https://*.trello.com; font-src 'self' https://cdnjs.cloudflare.com; frame-ancestors https://*.trello.com; object-src 'none'; base-uri 'self'; form-action 'self'; upgrade-insecure-requests;

  # X-Frame-Options
  X-Frame-Options: ALLOW-FROM https://trello.com

  # X-Content-Type-Options
  X-Content-Type-Options: nosniff

  # Referrer Policy
  Referrer-Policy: strict-origin-when-cross-origin

  # Permissions Policy
  Permissions-Policy: geolocation=(), microphone=(), camera=()

  # X-XSS-Protection (for older browsers that don't support CSP)
  X-XSS-Protection: 1; mode=block